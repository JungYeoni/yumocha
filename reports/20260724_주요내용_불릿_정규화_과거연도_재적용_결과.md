# [전처리] 주요내용 불릿 정규화 과거 연도 재적용 결과 — 2026-07-24

- 이슈: #34
- 대상: `data/interim/{시도}/{연도}_{시도}_세부사업_정제.csv`, `..._정제_long.csv`의 `주요내용`, `주요내용_정제` 컬럼

## 작업 배경

`dedup_label()`(공통 유틸)이 개선된 이후에도, 이미 저장된 과거 연도 산출물에는 개선 전 결과가 남아 있었다. LLM 재호출은 필요 없다고 판단해(체크포인트 파일 보존 확인) 규칙 기반 재적용만 수행했다.

## 잔존 현황 재확인

이슈 작성 시점에는 2016·2021은 해당 사례가 없다고 기록됐으나, 재확인 결과 두 연도 모두 잔존이 발견됐다. 2016 노트북을 여러 차례 재실행하면서 산출물이 다시 생성됐고, 그 과정에서 `주요내용_정제`(LLM 체크포인트 결과)에 LLM이 문장을 분리하며 자체적으로 삽입한 불릿(`•` 등)이 다시 남은 것으로 추정된다.

| 연도 | `주요내용` 잔존 | `주요내용_정제` 잔존 |
|---|---:|---:|
| 2016 | 0 | 650 |
| 2017 | 697 | 686 |
| 2021 | 114 | 110 |
| 2022 | 2,046 | 2,046 |
| 2023 | 21 | 7 |
| 2024 | 15 | 5 |

## 처리 방법

1. `BULLET_PATTERN`(`ㅇ○◦□▪•` 및 한글·영숫자 뒤가 아닌 `·`)으로 잔존 행을 먼저 식별
2. 재적용 전, 잔존 행 전체에 대해 `numbers_preserved()`로 숫자 시퀀스·순서가 보존되는지 검증 — **전 연도·전 컬럼 숫자 불일치 0건**
3. 잔존이 있는 파일만 `dedup_label()` 재적용 후 `utf-8-sig`로 재저장(wide·long 동일하게 처리)
4. 재적용 후 전체 파일에서 잔존 재검사 — **최종 잔존 0건**

재적용 과정은 `scripts/reapply_bullet_normalization.py`로 재현할 수 있다. 기본 실행은
파일을 변경하지 않는 검증 모드이며, `--write`를 지정하면 변경 전 파일을
`data/interim/.bullet_normalization_backups/{실행시각}/`에 백업한 뒤 원본을
원자적으로 교체한다.

```bash
# 잔존·숫자/수량 보존·wide/long 동기화 검증만 수행
uv run python scripts/reapply_bullet_normalization.py

# 백업 생성 후 실제 CSV에 반영
uv run python scripts/reapply_bullet_normalization.py --write
```

## 결과

- 총 154개 파일 갱신 (`주요내용_정제`: 2016~2024 중 잔존 있던 6개 연도, `주요내용`: 2017·2021·2022·2023·2024)
- 숫자·수량 표현 보존: 100% (검증 대상 전체 불일치 0건)
- 재현 스크립트 dry-run: 대상 wide·long 204개 검사, 추가 변경 0파일·0셀
- `pytest tests/` 138건 전체 통과

## 재현성 및 방법론적 한계

- **코드 버전**: `src/features/text_match.py`의 `dedup_label()` 개선 커밋(`7239336`, #19) 이후, 이번 재적용은 `b179d04`(#34) 기준으로 실행됨.
- **대상 데이터 범위**: `data/interim/{시도}/{연도}_*.csv`, 대상 연도 2016·2017·2021·2022·2023·2024. `data/interim/`은 `.gitignore` 대상이며, `--write` 실행 시 생성되는 백업 디렉터리는 변경된 파일만 보관하는 부분 백업이다. 전체 입력 데이터셋을 재현하려면 검사 대상 204개 파일의 경로·SHA-256 매니페스트 또는 전체 아카이브를 별도로 보관해야 한다.
- **평가 지표 정의**: "잔존 건수" = `BULLET_PATTERN` 매칭 행 수, "숫자/수량 보존" = `numbers_preserved()`(숫자 시퀀스·순서 일치) 및 `extract_quantities()`(수량 표현 일치) 결과.
- **분할/CV 미적용 사유**: 통계적 추정이나 모델 학습이 아니라 결정론적 규칙(`dedup_label()`) 재적용이므로 train/val/test 분할이나 CV fold 개념이 적용되지 않는다. 검증은 전수(204개 wide·long 파일) 재검사로 대체했다.
- **`BULLET_PATTERN` 미탐·오탐 위험**: 정규식 기반이라 패턴에 등록되지 않은 신규 불릿 문자(예: `‣`, `➤`)는 미탐 가능하다. 가운뎃점(`·`)은 한글·영숫자 뒤가 아닌 경우만 제거 대상으로 좁혀 오탐을 줄였지만, 문장 첫머리에 의미 있는 `·`가 온 경우(드묾) 오탐 여지는 남아 있다.
- **CSV 재직렬화 범위**: `주요내용`, `주요내용_정제` 두 컬럼의 값만 변경되고, 나머지 컬럼은 원문 그대로 재직렬화된다. 재적용 스크립트가 `dtype=str`·`keep_default_na=False`로 읽고 쓰도록 수정되어, `NA`/`NULL`/빈 값 등 텍스트가 결측치로 오인되어 손실되는 문제는 없음을 별도 테스트로 확인했다(`tests/test_reapply_bullet_normalization.py`).
- **핵심 가정**: (1) LLM 체크포인트 파일의 행 순서·스키마가 유지된다, (2) 규칙 기반 재적용만으로 충분하며 LLM 재호출은 불필요하다(이슈 #34 논의에서 확인) — 두 가정이 깨지면(예: 체크포인트 재구성) 재검증이 필요하다.
- **한계·잠재 편향**: 자동 검증은 숫자·수량 표현의 보존 여부만 확인하며, 불릿 제거 후 문장의 의미·가독성까지 검증하지 않는다. 표본에 대한 수동 검토는 수행하지 않았다.

## 참고

- `data/interim/`은 `.gitignore` 대상이라 이번 CSV 수정 자체는 git diff에 나타나지 않는다. 이 보고서가 작업 근거·검증 기록이다.
- macOS에서 일부 한글 파일명이 분해형(NFD)으로 저장된 경우도 누락하지 않도록 파일 탐색과 wide·long pair 검증 시 NFC 정규화를 적용한다.
- `run_checkpointed_refinement()`는 신규 LLM 응답뿐 아니라 재사용하는 기존 체크포인트의 `주요내용_정제`에도 `dedup_label()`을 적용한다. 정규화로 값이 바뀌고 신규 호출 대상이 없으면 체크포인트 자체도 즉시 다시 저장하므로, 노트북 재실행 시 과거 불릿이 산출물로 되돌아오는 문제를 방지한다.
