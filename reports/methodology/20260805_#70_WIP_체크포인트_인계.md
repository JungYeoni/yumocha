# #70 구조환경지표 결측 처리 WIP 체크포인트 인계

## 체크포인트 목적

- 브랜치: `feat/20260804_#70_구조환경지표_결측처리_실제패널_적용`
- Draft PR: `#89`
- 체크포인트 시작 HEAD: `429a324715dbb10f2a39b99450186d450b30701c`
- 범위: 가족친화 인증기업 비율 공식 관측 복원·2024 오류 정정, 보간 전 기준 패널 반영, 결측 정책 매핑 갱신, 관련 QA와 테스트
- 상태: **WIP**. 이 체크포인트는 #70 완료나 처리 완료 패널 생성을 의미하지 않는다.

## 현재 구현 상태

1. 공식 2020·2021년 가족친화 인증기업·기관 명단으로 17개 시도×2개 연도 34개 값을 복원했다.
2. 34개 값은 `공식 원자료 직접 복원`, `관측`으로 표시하며 보간값이나 대체값으로 취급하지 않는다.
3. 2024년 주소 문자열 부분일치 오류를 재현하고 전국·대구·광주 3개 값을 공식 집계로 정정했다.
4. 보간 전 기준 패널은 4,536개 키를 유지하며 관측 3,300건, 미관측 1,236건이다.
5. 결측 정책 매핑은 현재 미관측 1,236건을 대상으로 다시 생성됐다.
6. #82의 17개 시도 차단 결측은 가족친화 51건과 가사분담 성평등 인식 68건, 합계 119건이다.
7. `src/features/structural_missing.py`는 실행하지 않았고 본계열·대안 처리 완료 패널은 생성하지 않았다.

## 가족친화 인증기업 비율

### 2020·2021 직접 관측

- 2020년: 17개 시도, 공식 분자 합계 4,340
- 2021년: 17개 시도, 공식 분자 합계 4,918
- 산식: `(연도 말 유효 인증기업·기관 수 / 같은 연도 전산업·전규모 사업체 수) * 100`
- canonical 반영표: `reports/20260804_가족친화_공식관측_반영값.csv`
- 패널 반영 감사표: `reports/20260804_가족친화_공식관측_패널반영_QA.csv`

### 2024 공식 정정

| 지역 | 공식 분자 | 공식 비율 |
|---|---:|---:|
| 전국 | 6,502 | 0.3088699644148738 |
| 대구 | 218 | 0.2389957792029820 |
| 광주 | 140 | 0.2407980736154111 |

- 공식 파일: `가족친화인증기업현황_20241231.csv.xlsx`
- 파일 크기: 242,330 bytes
- SHA-256: `6026921d157c499156d6ddbce820833c6f96a6709b3814e940dae5a47f3d4696`
- 공식 17개 시도 합계: 6,502
- 유형 합계: 대기업 784, 중소기업 4,552, 공공기관 1,166
- 기존 6,581은 `부산광역시 해운대구` 59행이 대구에, `경기도 광주시` 20행이 광주에 중복 배정된 결과다.
- 공식 집계는 공식 `지역` 열을 우선한다. 주소 정규화는 접두어 기준이며 주소 전체 부분일치는 오류 재현 QA에서만 사용한다.

## 차단 결측

- 가족친화 17개 시도 2016·2017·2019년 51건: 값 결측, `pending_review`, `block_imputation=True`
- 가사분담 성평등 인식 17개 시도 68건: 차단 유지
- #82 17개 시도 차단 합계: 119건
- 전체 정책 매핑의 차단은 146건이다. 119건 외에 전국 가족친화 5건, 전국 가사분담 4건, 전국 청년층 정규직 9건, 전국 근로시간 9건이 포함된다.
- 전국 가족친화 2020·2021은 이번 34건 직접 복원 범위에 포함되지 않는다.

## 현재 생성 흐름

1. `scripts/build_family_friendly_candidates.py`가 공식 원본과 분모를 검증하고 후보·공식 관측 반영표·2024 원인 QA를 생성한다.
2. `notebooks/20260804_EDA_구조환경지표_28개_보간전_통합패널.ipynb`가 승인 입력 8개와 공식 관측 반영표 37개 키를 읽어 보간 전 기준 패널과 패널 QA를 생성한다.
3. `scripts/build_structural_missing_policy.py`가 갱신된 기준 패널을 읽어 결측 정책 전수 매핑과 정책 QA를 생성한다.

`data/raw/`와 `data/processed/`는 Git 무시 대상이다. 공식 원본은 raw에 원형으로 보관되며, 기준 패널은 노트북으로 재생성한다.

## 알려진 미완료 사항

다음 항목은 이 체크포인트에서 의도적으로 해결하지 않았다.

1. 가족친화 생성 스크립트가 정정된 기준 패널을 검증하고, 기준 패널 노트북은 생성 스크립트의 공식 관측 반영표를 요구하는 순환 의존
2. clean checkout에서 공식 원본부터 최초 재생성이 가능한지에 대한 보장
3. 노트북의 패널 적용 로직과 `apply_confirmed_observations()`의 단일화
4. 노트북을 대체할 독립 패널 생성 스크립트
5. 2024 공식 메타데이터의 빈 게시물 URL·다운로드 URL·다운로드일 보완
6. 본계열·대안 처리 완료 패널 생성 및 민감도 비교
7. `structural_missing.py` 실제 적용과 대체 셀 감사표 생성

특히 현재 코드에서 `scripts/build_family_friendly_candidates.py`를 old 기준 패널에 바로 실행하면 2024 패널 일치 검증이 공식 반영표 출력 전에 실패할 수 있다. 다음 작업자는 이 부트스트랩 순서를 먼저 정리해야 한다.

## 검증 상태

체크포인트 직전 다음 최소 검증을 실행했다.

```text
git diff --check
ruff check src/ tests/ scripts/
ruff format --check src/ tests/ scripts/
python -m pytest tests/test_family_friendly_candidates.py tests/test_structural_missing_policy.py
```

- `git diff --check`: PASS. 내용 오류는 없고 일부 파일의 향후 LF→CRLF 경고만 출력됨
- Ruff check: PASS
- Ruff format check: 78 files already formatted
- 집중 테스트: 26 passed, 1 warning
- 경고: pytest가 `.pytest_cache`를 생성하지 못했다는 접근 권한 경고이며 테스트 결과에는 영향이 없다.
- WIP 커밋·push 뒤 전체 `tests/`를 추가 실행했다. 환경성 실패 두 건과 최종 통과 내역은 아래에 기록한다.

기존 생성 결과에는 가족친화 QA 109/109 PASS, 정책 매핑 QA 83/83 PASS, 패널 노트북 QA 38/38 PASS가 기록돼 있다. 이는 이번 체크포인트에서 생성 스크립트나 노트북을 재실행해 확인한 결과가 아니라 현재 보존된 산출물의 기록이다.

### 전체 테스트 추가 검증

첫 실행 명령:

```text
python -m pytest tests/
```

- PATH의 시스템 Python 3.12가 선택됐다.
- 수집 중 20개 모듈에서 오류가 발생해 중단됐다.
- 오류 모듈: `test_apply_2021_semantic_corrections.py`, `test_apply_2023_blank_input_recovery.py`, `test_apply_2023_semantic_corrections.py`, `test_apply_2024_semantic_corrections.py`, `test_audit_llm_semantic_preservation.py`, `test_build_2016_2024_review_workbook.py`, `test_build_finance_review_master.py`, `test_build_grouped_finance_review_workbook.py`, `test_build_semantic_change_impact.py`, `test_features.py`, `test_llm_refine.py`, `test_predict_tfidf_area_classifier.py`, `test_reapply_bullet_normalization.py`, `test_similarity_grouping.py`, `test_text_match.py`, `test_tfidf_area_classifier.py`, `test_tfidf_review_workbook.py`, `test_train_tfidf_area_classifier.py`, `test_verify_2023_hallucination_recovery.py`, `test_visualization.py`
- 원인: 프로젝트 가상환경이 아닌 시스템 Python을 사용해 `rapidfuzz`, `scikit-learn`, `openpyxl`, `joblib`, `matplotlib` 의존성이 없었다. 코드 수정이나 의존성 설치는 하지 않았다.

두 번째 실행 명령:

```text
.\.venv\Scripts\python.exe -m pytest tests/
```

- 프로젝트 의존성은 정상 로드됐지만 결과는 `1 failed, 272 passed, 59 errors, 2 warnings`였다.
- 59개 setup 오류 원인: 기본 Windows 임시 경로 `%LOCALAPPDATA%\Temp\pytest-of-<USER>`에 접근할 수 없어 `tmp_path` 생성이 실패했다.
- 실패 테스트: `tests/test_visualization.py::test_structural_overview_uses_regional_median_when_nationwide_is_missing`
- 실패 원인: Matplotlib가 GUI Tk 백엔드를 선택했으나 현재 Python 배포판에서 사용 가능한 `init.tcl`을 찾지 못했다.
- 애플리케이션 로직을 수정하지 않고 테스트 실행 환경만 격리했다.

최종 실행 명령:

```powershell
$env:MPLBACKEND='Agg'
.\.venv\Scripts\python.exe -m pytest tests/ --basetemp .test_tmp_final -p no:cacheprovider
```

- 결과: **332 passed, 1 warning in 4.85s**
- 경고: Seaborn `PendingDeprecationWarning` 1건
- 저장소 내부 테스트 임시 디렉터리 `.test_tmp_final`은 실행 후 제거했다.
- 노트북과 생성 스크립트는 재실행하지 않았다.

## 다음 작업자 권장 순서

1. clean checkout 부트스트랩 관점에서 공식 관측 반영표와 기준 패널의 의존 방향을 정리한다.
2. 가족친화 원본 검증·37개 반영표 생성과 패널 반영을 재현한다.
3. 노트북을 클린 커널로 실행하고 4,536개 키·관측 3,300·미관측 1,236을 확인한다.
4. 정책 매핑을 재생성하고 시도 차단 119건과 전체 차단 146건을 확인한다.
5. Ruff, 집중 테스트, 전체 `python -m pytest tests/`를 실행한다.
6. 그 후에만 `structural_missing.py` 적용과 본계열·대안 패널 생성을 진행한다.

Draft PR #89를 Ready로 전환하거나 병합하지 말고, #70 완료 조건을 충족한 뒤 별도로 판단한다.
